/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: b0f689809eb546569bb1ab1210d84798d36f15339d440967024ed48bed9da095.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated raw/compatibility dispatch arms: `cache_policy` intrinsics.

use super::helpers::require_arity;
use crate::error::TranslationResult;
use crate::translator::{rvalue, terminator::helpers, values::ValueMap};
use dialect_nvvm::ops::{
    CreatepolicyFractionalEvictFirstEvictFirstOp, CreatepolicyFractionalEvictFirstOp,
    CreatepolicyFractionalEvictLastEvictFirstOp, CreatepolicyFractionalEvictLastOp,
    CreatepolicyFractionalEvictNormalEvictFirstOp, CreatepolicyFractionalEvictNormalOp,
    CreatepolicyFractionalEvictUnchangedEvictFirstOp, CreatepolicyFractionalEvictUnchangedOp,
};
use pliron::basic_block::BasicBlock;
use pliron::context::{Context, Ptr};
use pliron::location::{Located, Location};
use pliron::operation::Operation;
use rustc_public::mir;

#[allow(clippy::too_many_arguments)]
pub(super) fn try_dispatch(
    ctx: &mut Context,
    body: &mir::Body,
    name: &str,
    args: &[mir::Operand],
    destination: &mir::Place,
    target: &Option<usize>,
    block_ptr: Ptr<BasicBlock>,
    prev_op: Option<Ptr<Operation>>,
    value_map: &mut ValueMap,
    block_map: &[Ptr<BasicBlock>],
    loc: Location,
) -> TranslationResult<Option<Ptr<Operation>>> {
    match name {
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1041"
        | "cuda_device::cache_policy::createpolicy_fractional_evict_first" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = CreatepolicyFractionalEvictFirstOp::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1041");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "createpolicy_fractional_evict_first call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1045"
        | "cuda_device::cache_policy::createpolicy_fractional_evict_first_evict_first" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = CreatepolicyFractionalEvictFirstEvictFirstOp::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1045");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "createpolicy_fractional_evict_first_evict_first call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1039"
        | "cuda_device::cache_policy::createpolicy_fractional_evict_last" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = CreatepolicyFractionalEvictLastOp::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1039");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "createpolicy_fractional_evict_last call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1043"
        | "cuda_device::cache_policy::createpolicy_fractional_evict_last_evict_first" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = CreatepolicyFractionalEvictLastEvictFirstOp::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1043");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "createpolicy_fractional_evict_last_evict_first call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1040"
        | "cuda_device::cache_policy::createpolicy_fractional_evict_normal" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = CreatepolicyFractionalEvictNormalOp::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1040");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "createpolicy_fractional_evict_normal call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1044"
        | "cuda_device::cache_policy::createpolicy_fractional_evict_normal_evict_first" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = CreatepolicyFractionalEvictNormalEvictFirstOp::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1044");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "createpolicy_fractional_evict_normal_evict_first call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1042"
        | "cuda_device::cache_policy::createpolicy_fractional_evict_unchanged" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = CreatepolicyFractionalEvictUnchangedOp::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1042");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "createpolicy_fractional_evict_unchanged call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1046"
        | "cuda_device::cache_policy::createpolicy_fractional_evict_unchanged_evict_first" => {
            require_arity(name, args.len(), 1, &loc)?;
            let (arg0, last_op) = rvalue::translate_operand(
                ctx,
                body,
                &args[0],
                value_map,
                block_ptr,
                prev_op,
                loc.clone(),
            )?;
            let intrinsic = CreatepolicyFractionalEvictUnchangedEvictFirstOp::build(ctx, arg0);
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1046");
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let result = intrinsic.deref(ctx).get_result(0);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                intrinsic,
                value_map,
                block_map,
                loc,
                "createpolicy_fractional_evict_unchanged_evict_first call without target block",
            )?))
        }
        _ => Ok(None),
    }
}
