/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: f3d74cc3c2ba8ee0d8c4678a2ec3bca8c5bdf3e3a7480181f3e3fd12015f6790.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Generated raw/compatibility dispatch arms: `tcgen05_ld_red` intrinsics.

use super::helpers::require_arity;
use crate::error::{TranslationErr, TranslationResult};
use crate::translator::{rvalue, terminator::helpers, types, values::ValueMap};
use dialect_mir::{
    ops::{MirConstantOp, MirConstructArrayOp, MirConstructStructOp, MirConstructTupleOp},
    types::{MirArrayType, MirTupleType},
};
use dialect_nvvm::ops::{
    Tcgen05LdRed16x32bx2X2MaxAbsF32Op, Tcgen05LdRed16x32bx2X2MaxAbsNanF32Op,
    Tcgen05LdRed16x32bx2X2MaxF32Op, Tcgen05LdRed16x32bx2X2MaxNanF32Op,
    Tcgen05LdRed16x32bx2X2MaxS32Op, Tcgen05LdRed16x32bx2X2MaxU32Op,
    Tcgen05LdRed16x32bx2X2MinAbsF32Op, Tcgen05LdRed16x32bx2X2MinAbsNanF32Op,
    Tcgen05LdRed16x32bx2X2MinF32Op, Tcgen05LdRed16x32bx2X2MinNanF32Op,
    Tcgen05LdRed16x32bx2X2MinS32Op, Tcgen05LdRed16x32bx2X2MinU32Op,
    Tcgen05LdRed16x32bx2X4MaxAbsF32Op, Tcgen05LdRed16x32bx2X4MaxAbsNanF32Op,
    Tcgen05LdRed16x32bx2X4MaxF32Op, Tcgen05LdRed16x32bx2X4MaxNanF32Op,
    Tcgen05LdRed16x32bx2X4MaxS32Op, Tcgen05LdRed16x32bx2X4MaxU32Op,
    Tcgen05LdRed16x32bx2X4MinAbsF32Op, Tcgen05LdRed16x32bx2X4MinAbsNanF32Op,
    Tcgen05LdRed16x32bx2X4MinF32Op, Tcgen05LdRed16x32bx2X4MinNanF32Op,
    Tcgen05LdRed16x32bx2X4MinS32Op, Tcgen05LdRed16x32bx2X4MinU32Op,
    Tcgen05LdRed16x32bx2X8MaxAbsF32Op, Tcgen05LdRed16x32bx2X8MaxAbsNanF32Op,
    Tcgen05LdRed16x32bx2X8MaxF32Op, Tcgen05LdRed16x32bx2X8MaxNanF32Op,
    Tcgen05LdRed16x32bx2X8MaxS32Op, Tcgen05LdRed16x32bx2X8MaxU32Op,
    Tcgen05LdRed16x32bx2X8MinAbsF32Op, Tcgen05LdRed16x32bx2X8MinAbsNanF32Op,
    Tcgen05LdRed16x32bx2X8MinF32Op, Tcgen05LdRed16x32bx2X8MinNanF32Op,
    Tcgen05LdRed16x32bx2X8MinS32Op, Tcgen05LdRed16x32bx2X8MinU32Op,
    Tcgen05LdRed16x32bx2X16MaxAbsF32Op, Tcgen05LdRed16x32bx2X16MaxAbsNanF32Op,
    Tcgen05LdRed16x32bx2X16MaxF32Op, Tcgen05LdRed16x32bx2X16MaxNanF32Op,
    Tcgen05LdRed16x32bx2X16MaxS32Op, Tcgen05LdRed16x32bx2X16MaxU32Op,
    Tcgen05LdRed16x32bx2X16MinAbsF32Op, Tcgen05LdRed16x32bx2X16MinAbsNanF32Op,
    Tcgen05LdRed16x32bx2X16MinF32Op, Tcgen05LdRed16x32bx2X16MinNanF32Op,
    Tcgen05LdRed16x32bx2X16MinS32Op, Tcgen05LdRed16x32bx2X16MinU32Op,
    Tcgen05LdRed16x32bx2X32MaxAbsF32Op, Tcgen05LdRed16x32bx2X32MaxAbsNanF32Op,
    Tcgen05LdRed16x32bx2X32MaxF32Op, Tcgen05LdRed16x32bx2X32MaxNanF32Op,
    Tcgen05LdRed16x32bx2X32MaxS32Op, Tcgen05LdRed16x32bx2X32MaxU32Op,
    Tcgen05LdRed16x32bx2X32MinAbsF32Op, Tcgen05LdRed16x32bx2X32MinAbsNanF32Op,
    Tcgen05LdRed16x32bx2X32MinF32Op, Tcgen05LdRed16x32bx2X32MinNanF32Op,
    Tcgen05LdRed16x32bx2X32MinS32Op, Tcgen05LdRed16x32bx2X32MinU32Op,
    Tcgen05LdRed16x32bx2X64MaxAbsF32Op, Tcgen05LdRed16x32bx2X64MaxAbsNanF32Op,
    Tcgen05LdRed16x32bx2X64MaxF32Op, Tcgen05LdRed16x32bx2X64MaxNanF32Op,
    Tcgen05LdRed16x32bx2X64MaxS32Op, Tcgen05LdRed16x32bx2X64MaxU32Op,
    Tcgen05LdRed16x32bx2X64MinAbsF32Op, Tcgen05LdRed16x32bx2X64MinAbsNanF32Op,
    Tcgen05LdRed16x32bx2X64MinF32Op, Tcgen05LdRed16x32bx2X64MinNanF32Op,
    Tcgen05LdRed16x32bx2X64MinS32Op, Tcgen05LdRed16x32bx2X64MinU32Op,
    Tcgen05LdRed16x32bx2X128MaxAbsF32Op, Tcgen05LdRed16x32bx2X128MaxAbsNanF32Op,
    Tcgen05LdRed16x32bx2X128MaxF32Op, Tcgen05LdRed16x32bx2X128MaxNanF32Op,
    Tcgen05LdRed16x32bx2X128MaxS32Op, Tcgen05LdRed16x32bx2X128MaxU32Op,
    Tcgen05LdRed16x32bx2X128MinAbsF32Op, Tcgen05LdRed16x32bx2X128MinAbsNanF32Op,
    Tcgen05LdRed16x32bx2X128MinF32Op, Tcgen05LdRed16x32bx2X128MinNanF32Op,
    Tcgen05LdRed16x32bx2X128MinS32Op, Tcgen05LdRed16x32bx2X128MinU32Op,
    Tcgen05LdRed32x32bX2MaxAbsF32Op, Tcgen05LdRed32x32bX2MaxAbsNanF32Op,
    Tcgen05LdRed32x32bX2MaxF32Op, Tcgen05LdRed32x32bX2MaxNanF32Op, Tcgen05LdRed32x32bX2MaxS32Op,
    Tcgen05LdRed32x32bX2MaxU32Op, Tcgen05LdRed32x32bX2MinAbsF32Op,
    Tcgen05LdRed32x32bX2MinAbsNanF32Op, Tcgen05LdRed32x32bX2MinF32Op,
    Tcgen05LdRed32x32bX2MinNanF32Op, Tcgen05LdRed32x32bX2MinS32Op, Tcgen05LdRed32x32bX2MinU32Op,
    Tcgen05LdRed32x32bX4MaxAbsF32Op, Tcgen05LdRed32x32bX4MaxAbsNanF32Op,
    Tcgen05LdRed32x32bX4MaxF32Op, Tcgen05LdRed32x32bX4MaxNanF32Op, Tcgen05LdRed32x32bX4MaxS32Op,
    Tcgen05LdRed32x32bX4MaxU32Op, Tcgen05LdRed32x32bX4MinAbsF32Op,
    Tcgen05LdRed32x32bX4MinAbsNanF32Op, Tcgen05LdRed32x32bX4MinF32Op,
    Tcgen05LdRed32x32bX4MinNanF32Op, Tcgen05LdRed32x32bX4MinS32Op, Tcgen05LdRed32x32bX4MinU32Op,
    Tcgen05LdRed32x32bX8MaxAbsF32Op, Tcgen05LdRed32x32bX8MaxAbsNanF32Op,
    Tcgen05LdRed32x32bX8MaxF32Op, Tcgen05LdRed32x32bX8MaxNanF32Op, Tcgen05LdRed32x32bX8MaxS32Op,
    Tcgen05LdRed32x32bX8MaxU32Op, Tcgen05LdRed32x32bX8MinAbsF32Op,
    Tcgen05LdRed32x32bX8MinAbsNanF32Op, Tcgen05LdRed32x32bX8MinF32Op,
    Tcgen05LdRed32x32bX8MinNanF32Op, Tcgen05LdRed32x32bX8MinS32Op, Tcgen05LdRed32x32bX8MinU32Op,
    Tcgen05LdRed32x32bX16MaxAbsF32Op, Tcgen05LdRed32x32bX16MaxAbsNanF32Op,
    Tcgen05LdRed32x32bX16MaxF32Op, Tcgen05LdRed32x32bX16MaxNanF32Op, Tcgen05LdRed32x32bX16MaxS32Op,
    Tcgen05LdRed32x32bX16MaxU32Op, Tcgen05LdRed32x32bX16MinAbsF32Op,
    Tcgen05LdRed32x32bX16MinAbsNanF32Op, Tcgen05LdRed32x32bX16MinF32Op,
    Tcgen05LdRed32x32bX16MinNanF32Op, Tcgen05LdRed32x32bX16MinS32Op, Tcgen05LdRed32x32bX16MinU32Op,
    Tcgen05LdRed32x32bX32MaxAbsF32Op, Tcgen05LdRed32x32bX32MaxAbsNanF32Op,
    Tcgen05LdRed32x32bX32MaxF32Op, Tcgen05LdRed32x32bX32MaxNanF32Op, Tcgen05LdRed32x32bX32MaxS32Op,
    Tcgen05LdRed32x32bX32MaxU32Op, Tcgen05LdRed32x32bX32MinAbsF32Op,
    Tcgen05LdRed32x32bX32MinAbsNanF32Op, Tcgen05LdRed32x32bX32MinF32Op,
    Tcgen05LdRed32x32bX32MinNanF32Op, Tcgen05LdRed32x32bX32MinS32Op, Tcgen05LdRed32x32bX32MinU32Op,
    Tcgen05LdRed32x32bX64MaxAbsF32Op, Tcgen05LdRed32x32bX64MaxAbsNanF32Op,
    Tcgen05LdRed32x32bX64MaxF32Op, Tcgen05LdRed32x32bX64MaxNanF32Op, Tcgen05LdRed32x32bX64MaxS32Op,
    Tcgen05LdRed32x32bX64MaxU32Op, Tcgen05LdRed32x32bX64MinAbsF32Op,
    Tcgen05LdRed32x32bX64MinAbsNanF32Op, Tcgen05LdRed32x32bX64MinF32Op,
    Tcgen05LdRed32x32bX64MinNanF32Op, Tcgen05LdRed32x32bX64MinS32Op, Tcgen05LdRed32x32bX64MinU32Op,
    Tcgen05LdRed32x32bX128MaxAbsF32Op, Tcgen05LdRed32x32bX128MaxAbsNanF32Op,
    Tcgen05LdRed32x32bX128MaxF32Op, Tcgen05LdRed32x32bX128MaxNanF32Op,
    Tcgen05LdRed32x32bX128MaxS32Op, Tcgen05LdRed32x32bX128MaxU32Op,
    Tcgen05LdRed32x32bX128MinAbsF32Op, Tcgen05LdRed32x32bX128MinAbsNanF32Op,
    Tcgen05LdRed32x32bX128MinF32Op, Tcgen05LdRed32x32bX128MinNanF32Op,
    Tcgen05LdRed32x32bX128MinS32Op, Tcgen05LdRed32x32bX128MinU32Op,
};
use pliron::basic_block::BasicBlock;
use pliron::builtin::types::{FP32Type, IntegerType, Signedness};
use pliron::context::{Context, Ptr};
use pliron::input_err;
use pliron::location::{Located, Location};
use pliron::op::Op;
use pliron::operation::Operation;
use pliron::r#type::{TypeHandle, Typed};
use pliron::value::Value;
use rustc_public::mir;

#[allow(clippy::too_many_arguments)]
pub(super) fn try_dispatch(
    ctx: &mut Context,
    body: &mir::Body,
    name: &str,
    args: &[mir::Operand],
    destination: &mir::Place,
    target: &Option<usize>,
    block_ptr: Ptr<BasicBlock>,
    prev_op: Option<Ptr<Operation>>,
    value_map: &mut ValueMap,
    block_map: &[Ptr<BasicBlock>],
    loc: Location,
) -> TranslationResult<Option<Ptr<Operation>>> {
    match name {
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1213"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_max_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1213");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1214"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_max_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1214");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1211"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_max_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1211");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1212"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_max_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1212");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1206"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_max_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1206");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1204"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_max_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1204");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1209"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_min_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1209");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1210"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_min_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1210");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1207"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_min_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1207");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1208"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_min_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1208");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1205"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_min_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1205");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1203"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x128_min_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X128MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1203");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x128_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1177"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_max_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1177");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1178"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_max_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1178");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1175"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_max_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1175");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1176"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_max_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1176");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1170"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_max_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1170");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1168"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_max_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1168");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1173"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_min_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1173");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1174"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_min_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1174");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1171"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_min_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1171");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1172"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_min_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1172");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1169"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_min_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1169");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1167"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x16_min_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X16MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1167");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x16_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1141"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_max_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1141");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1142"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_max_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1142");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1139"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_max_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1139");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1140"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_max_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1140");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1134"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_max_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1134");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1132"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_max_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1132");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1137"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_min_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1137");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1138"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_min_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1138");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1135"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_min_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1135");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1136"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_min_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1136");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1133"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_min_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1133");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1131"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x2_min_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X2MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1131");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x2_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1189"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_max_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1189");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1190"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_max_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1190");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1187"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_max_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1187");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1188"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_max_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1188");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1182"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_max_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1182");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1180"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_max_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1180");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1185"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_min_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1185");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1186"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_min_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1186");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1183"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_min_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1183");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1184"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_min_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1184");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1181"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_min_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1181");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1179"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x32_min_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X32MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1179");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x32_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1153"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_max_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1153");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1154"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_max_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1154");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1151"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_max_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1151");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1152"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_max_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1152");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1146"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_max_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1146");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1144"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_max_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1144");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1149"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_min_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1149");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1150"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_min_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1150");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1147"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_min_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1147");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1148"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_min_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1148");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1145"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_min_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1145");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1143"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x4_min_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X4MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1143");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x4_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1201"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_max_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1201");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1202"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_max_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1202");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1199"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_max_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1199");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1200"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_max_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1200");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1194"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_max_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1194");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1192"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_max_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1192");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1197"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_min_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1197");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1198"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_min_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1198");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1195"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_min_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1195");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1196"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_min_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1196");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1193"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_min_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1193");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1191"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x64_min_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X64MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1191");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x64_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1165"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_max_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1165");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1166"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_max_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1166");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1163"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_max_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1163");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1164"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_max_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1164");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1158"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_max_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1158");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1156"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_max_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1156");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1161"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_min_abs_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1161");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1162"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_min_abs_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1162");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1159"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_min_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1159");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1160"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_min_nan_f32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1160");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1157"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_min_s32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1157");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1155"
        | "cuda_device::tcgen05::__tcgen05_ld_red_16x32bx2_x8_min_u32" => {
            require_arity(name, args.len(), 2, &loc)?;
            if !matches!(args.get(1), Some(mir::Operand::Constant(_))) {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must be a compile-time constant"
                            .to_owned()
                    )
                );
            }
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            if operands
                .get(1)
                .and_then(|value| value.defining_op())
                .and_then(|op| Operation::get_op::<MirConstantOp>(op, ctx))
                .is_none()
            {
                return input_err!(
                    loc,
                    TranslationErr::unsupported(
                        "tcgen05 16x32bx2 half-split offset must lower to a constant".to_owned()
                    )
                );
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed16x32bx2X8MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1155");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_16x32bx2_x8_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1129"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_max_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1129");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1130"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_max_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1130");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1127"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_max_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1127");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1128"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_max_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1128");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1122"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_max_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1122");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1120"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_max_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1120");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1125"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_min_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1125");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1126"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_min_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1126");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1123"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_min_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1123");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1124"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_min_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1124");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1121"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_min_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1121");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1119"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x128_min_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..129).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX128MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1119");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..128)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(128);
            let array_ty = MirArrayType::get(ctx, result_ty, 128);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x128_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1093"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_max_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1093");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1094"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_max_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1094");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1091"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_max_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1091");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1092"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_max_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1092");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1086"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_max_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1086");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1084"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_max_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1084");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1089"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_min_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1089");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1090"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_min_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1090");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1087"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_min_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1087");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1088"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_min_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1088");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1085"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_min_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1085");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1083"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x16_min_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..17).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX16MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1083");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..16)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(16);
            let array_ty = MirArrayType::get(ctx, result_ty, 16);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x16_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1057"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_max_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1057");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1058"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_max_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1058");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1055"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_max_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1055");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1056"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_max_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1056");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1050"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_max_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1050");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1048"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_max_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1048");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1053"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_min_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1053");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1054"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_min_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1054");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1051"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_min_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1051");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1052"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_min_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1052");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1049"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_min_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1049");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1047"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x2_min_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..3).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX2MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1047");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..2)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(2);
            let array_ty = MirArrayType::get(ctx, result_ty, 2);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x2_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1105"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_max_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1105");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1106"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_max_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1106");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1103"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_max_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1103");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1104"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_max_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1104");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1098"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_max_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1098");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1096"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_max_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1096");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1101"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_min_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1101");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1102"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_min_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1102");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1099"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_min_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1099");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1100"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_min_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1100");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1097"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_min_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1097");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1095"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x32_min_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..33).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX32MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1095");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..32)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(32);
            let array_ty = MirArrayType::get(ctx, result_ty, 32);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x32_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1069"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_max_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1069");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1070"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_max_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1070");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1067"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_max_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1067");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1068"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_max_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1068");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1062"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_max_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1062");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1060"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_max_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1060");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1065"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_min_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1065");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1066"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_min_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1066");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1063"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_min_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1063");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1064"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_min_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1064");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1061"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_min_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1061");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1059"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x4_min_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..5).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX4MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1059");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..4)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(4);
            let array_ty = MirArrayType::get(ctx, result_ty, 4);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x4_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1117"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_max_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1117");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1118"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_max_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1118");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1115"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_max_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1115");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1116"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_max_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1116");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1110"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_max_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1110");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1108"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_max_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1108");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1113"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_min_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1113");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1114"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_min_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1114");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1111"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_min_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1111");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1112"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_min_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1112");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1109"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_min_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1109");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1107"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x64_min_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..65).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX64MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1107");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..64)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(64);
            let array_ty = MirArrayType::get(ctx, result_ty, 64);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x64_min_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1081"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_max_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MaxAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1081");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_max_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1082"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_max_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MaxAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1082");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_max_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1079"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_max_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MaxF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1079");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_max_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1080"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_max_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MaxNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1080");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_max_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1074"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_max_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MaxS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1074");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_max_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1072"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_max_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MaxU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1072");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_max_u32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1077"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_min_abs_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MinAbsF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1077");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_min_abs_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1078"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_min_abs_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MinAbsNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1078");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_min_abs_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1075"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_min_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MinF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1075");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_min_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1076"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_min_nan_f32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = FP32Type::get(ctx).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MinNanF32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1076");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_min_nan_f32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1073"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_min_s32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Signed).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MinS32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1073");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_min_s32 call without target block",
            )?))
        }
        "cuda_intrinsics::__cuda_oxide_intrinsic_abi_v1::i1071"
        | "cuda_device::tcgen05::tcgen05_ld_red_32x32b_x8_min_u32" => {
            require_arity(name, args.len(), 1, &loc)?;
            let mut last_op = prev_op;
            let mut operands = Vec::with_capacity(args.len());
            for arg in args {
                let (value, translated) = rvalue::translate_operand(
                    ctx,
                    body,
                    arg,
                    value_map,
                    block_ptr,
                    last_op,
                    loc.clone(),
                )?;
                last_op = translated;
                operands.push(value);
            }
            let result_ty: TypeHandle = IntegerType::get(ctx, 32, Signedness::Unsigned).into();
            let result_types = (0..9).map(|_| result_ty).collect();
            let intrinsic = Operation::new(
                ctx,
                Tcgen05LdRed32x32bX8MinU32Op::get_concrete_op_info(),
                result_types,
                operands,
                vec![],
                0,
            );
            intrinsic.deref_mut(ctx).set_loc(loc.clone());
            helpers::set_generated_intrinsic_marker(ctx, intrinsic, "v1:i1071");
            let tuple_ty = types::translate_destination_type(ctx, body, destination, &loc)?;
            let registers_ty = {
                let ty = tuple_ty.deref(ctx);
                match ty.downcast_ref::<MirTupleType>() {
                    Some(tuple)
                        if tuple.get_types().len() == 2 && tuple.get_types()[1] == result_ty =>
                    {
                        tuple.get_types()[0]
                    }
                    _ => {
                        return input_err!(
                            loc.clone(),
                            TranslationErr::unsupported(
                                "tcgen05 reducing-load destination must be a (registers, reduction) tuple".to_owned()
                            )
                        );
                    }
                }
            };
            let (prepared_destination, prepared_last_op) = helpers::prepare_destination_write(
                ctx,
                body,
                destination,
                value_map,
                block_ptr,
                last_op,
                loc.clone(),
            )?;
            helpers::insert_op(ctx, intrinsic, block_ptr, prepared_last_op);
            let registers: Vec<Value> = (0..8)
                .map(|index| intrinsic.deref(ctx).get_result(index))
                .collect();
            let reduction = intrinsic.deref(ctx).get_result(8);
            let array_ty = MirArrayType::get(ctx, result_ty, 8);
            let array = Operation::new(
                ctx,
                MirConstructArrayOp::get_concrete_op_info(),
                vec![array_ty.into()],
                registers,
                vec![],
                0,
            );
            array.deref_mut(ctx).set_loc(loc.clone());
            array.insert_after(ctx, intrinsic);
            let array_result = array.deref(ctx).get_result(0);
            let (registers_value, registers_op) = if registers_ty == array_result.get_type(ctx) {
                (array_result, array)
            } else {
                let value = Operation::new(
                    ctx,
                    MirConstructStructOp::get_concrete_op_info(),
                    vec![registers_ty],
                    vec![array_result],
                    vec![],
                    0,
                );
                value.deref_mut(ctx).set_loc(loc.clone());
                value.insert_after(ctx, array);
                (value.deref(ctx).get_result(0), value)
            };
            let value = Operation::new(
                ctx,
                MirConstructTupleOp::get_concrete_op_info(),
                vec![tuple_ty],
                vec![registers_value, reduction],
                vec![],
                0,
            );
            value.deref_mut(ctx).set_loc(loc.clone());
            value.insert_after(ctx, registers_op);
            let result = value.deref(ctx).get_result(0);
            helpers::set_compiler_result_bundle_marker(ctx, value);
            Ok(Some(helpers::emit_prepared_result_and_goto(
                ctx,
                prepared_destination,
                result,
                target,
                block_ptr,
                value,
                value_map,
                block_map,
                loc,
                "tcgen05_ld_red_32x32b_x8_min_u32 call without target block",
            )?))
        }
        _ => Ok(None),
    }
}
