/*
 * SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 */

// @generated by cuda-intrinsics-gen 0.1.0. DO NOT EDIT.
// Catalog schema/version: 46/0.1.0; intrinsic ABI: v1; catalog SHA-256: b0f689809eb546569bb1ab1210d84798d36f15339d440967024ed48bed9da095.
// LLVM source: 16696adcd119e6ba9cc175207d984d7021211acb.

//! Structural operations for generated `createpolicy` L2 cache policies.

use pliron::{
    builtin::{
        op_interfaces::{NOpdsInterface, NResultsInterface},
        types::{FP32Type, IntegerType, Signedness},
    },
    common_traits::Verify,
    context::{Context, Ptr},
    location::Located,
    op::Op,
    operation::Operation,
    result::Error,
    r#type::Typed,
    value::Value,
    verify_err,
};
use pliron_derive::pliron_op;

fn is_f32(ctx: &Context, ty: pliron::r#type::TypeHandle) -> bool {
    ty.deref(ctx).downcast_ref::<FP32Type>().is_some()
}

fn is_i64(ctx: &Context, ty: pliron::r#type::TypeHandle) -> bool {
    ty.deref(ctx)
        .downcast_ref::<IntegerType>()
        .is_some_and(|integer| integer.width() == 64)
}

/// Creates an L2 cache policy that marks the given fraction of accessed lines evict-first and leaves the rest unchanged.
///
/// Lowers to `createpolicy.fractional.L2::evict_first.b64`.
#[pliron_op(
    name = "nvvm.createpolicy_fractional_evict_first",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct CreatepolicyFractionalEvictFirstOp;

impl CreatepolicyFractionalEvictFirstOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, fraction: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![fraction],
            vec![],
            0,
        )
    }
}

impl Verify for CreatepolicyFractionalEvictFirstOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_first requires exactly 1 operand and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_first requires an f32 fraction and a 64-bit policy result"
            );
        }
        Ok(())
    }
}

/// Creates an L2 cache policy that marks the given fraction of accessed lines evict-first and marks the rest evict-first.
///
/// Lowers to `createpolicy.fractional.L2::evict_first.L2::evict_first.b64`.
#[pliron_op(
    name = "nvvm.createpolicy_fractional_evict_first_evict_first",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct CreatepolicyFractionalEvictFirstEvictFirstOp;

impl CreatepolicyFractionalEvictFirstEvictFirstOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, fraction: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![fraction],
            vec![],
            0,
        )
    }
}

impl Verify for CreatepolicyFractionalEvictFirstEvictFirstOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_first_evict_first requires exactly 1 operand and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_first_evict_first requires an f32 fraction and a 64-bit policy result"
            );
        }
        Ok(())
    }
}

/// Creates an L2 cache policy that marks the given fraction of accessed lines evict-last and leaves the rest unchanged.
///
/// Lowers to `createpolicy.fractional.L2::evict_last.b64`.
#[pliron_op(
    name = "nvvm.createpolicy_fractional_evict_last",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct CreatepolicyFractionalEvictLastOp;

impl CreatepolicyFractionalEvictLastOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, fraction: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![fraction],
            vec![],
            0,
        )
    }
}

impl Verify for CreatepolicyFractionalEvictLastOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_last requires exactly 1 operand and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_last requires an f32 fraction and a 64-bit policy result"
            );
        }
        Ok(())
    }
}

/// Creates an L2 cache policy that marks the given fraction of accessed lines evict-last and marks the rest evict-first.
///
/// Lowers to `createpolicy.fractional.L2::evict_last.L2::evict_first.b64`.
#[pliron_op(
    name = "nvvm.createpolicy_fractional_evict_last_evict_first",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct CreatepolicyFractionalEvictLastEvictFirstOp;

impl CreatepolicyFractionalEvictLastEvictFirstOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, fraction: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![fraction],
            vec![],
            0,
        )
    }
}

impl Verify for CreatepolicyFractionalEvictLastEvictFirstOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_last_evict_first requires exactly 1 operand and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_last_evict_first requires an f32 fraction and a 64-bit policy result"
            );
        }
        Ok(())
    }
}

/// Creates an L2 cache policy that marks the given fraction of accessed lines evict-normal and leaves the rest unchanged.
///
/// Lowers to `createpolicy.fractional.L2::evict_normal.b64`.
#[pliron_op(
    name = "nvvm.createpolicy_fractional_evict_normal",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct CreatepolicyFractionalEvictNormalOp;

impl CreatepolicyFractionalEvictNormalOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, fraction: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![fraction],
            vec![],
            0,
        )
    }
}

impl Verify for CreatepolicyFractionalEvictNormalOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_normal requires exactly 1 operand and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_normal requires an f32 fraction and a 64-bit policy result"
            );
        }
        Ok(())
    }
}

/// Creates an L2 cache policy that marks the given fraction of accessed lines evict-normal and marks the rest evict-first.
///
/// Lowers to `createpolicy.fractional.L2::evict_normal.L2::evict_first.b64`.
#[pliron_op(
    name = "nvvm.createpolicy_fractional_evict_normal_evict_first",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct CreatepolicyFractionalEvictNormalEvictFirstOp;

impl CreatepolicyFractionalEvictNormalEvictFirstOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, fraction: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![fraction],
            vec![],
            0,
        )
    }
}

impl Verify for CreatepolicyFractionalEvictNormalEvictFirstOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_normal_evict_first requires exactly 1 operand and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_normal_evict_first requires an f32 fraction and a 64-bit policy result"
            );
        }
        Ok(())
    }
}

/// Creates an L2 cache policy that leaves the eviction priority of all accessed lines unchanged.
///
/// Lowers to `createpolicy.fractional.L2::evict_unchanged.b64`.
#[pliron_op(
    name = "nvvm.createpolicy_fractional_evict_unchanged",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct CreatepolicyFractionalEvictUnchangedOp;

impl CreatepolicyFractionalEvictUnchangedOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, fraction: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![fraction],
            vec![],
            0,
        )
    }
}

impl Verify for CreatepolicyFractionalEvictUnchangedOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_unchanged requires exactly 1 operand and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_unchanged requires an f32 fraction and a 64-bit policy result"
            );
        }
        Ok(())
    }
}

/// Creates an L2 cache policy that leaves the given fraction of accessed lines unchanged and marks the rest evict-first.
///
/// Lowers to `createpolicy.fractional.L2::evict_unchanged.L2::evict_first.b64`.
#[pliron_op(
    name = "nvvm.createpolicy_fractional_evict_unchanged_evict_first",
    format,
    interfaces = [NOpdsInterface<1>, NResultsInterface<1>],
)]
pub struct CreatepolicyFractionalEvictUnchangedEvictFirstOp;

impl CreatepolicyFractionalEvictUnchangedEvictFirstOp {
    pub fn new(op: Ptr<Operation>) -> Self {
        Self { op }
    }

    pub fn build(ctx: &mut Context, fraction: Value) -> Ptr<Operation> {
        let result_ty = IntegerType::get(ctx, 64, Signedness::Unsigned);
        Operation::new(
            ctx,
            Self::get_concrete_op_info(),
            vec![result_ty.into()],
            vec![fraction],
            vec![],
            0,
        )
    }
}

impl Verify for CreatepolicyFractionalEvictUnchangedEvictFirstOp {
    fn verify(&self, ctx: &Context) -> Result<(), Error> {
        let op = self.get_operation().deref(ctx);
        if op.get_num_operands() != 1 || op.get_num_results() != 1 {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_unchanged_evict_first requires exactly 1 operand and one result"
            );
        }
        if !is_f32(ctx, op.get_operand(0).get_type(ctx))
            || !is_i64(ctx, op.get_result(0).get_type(ctx))
        {
            return verify_err!(
                op.loc(),
                "nvvm.createpolicy_fractional_evict_unchanged_evict_first requires an f32 fraction and a 64-bit policy result"
            );
        }
        Ok(())
    }
}

pub(super) fn register(ctx: &mut Context) {
    CreatepolicyFractionalEvictFirstOp::register(ctx);
    CreatepolicyFractionalEvictFirstEvictFirstOp::register(ctx);
    CreatepolicyFractionalEvictLastOp::register(ctx);
    CreatepolicyFractionalEvictLastEvictFirstOp::register(ctx);
    CreatepolicyFractionalEvictNormalOp::register(ctx);
    CreatepolicyFractionalEvictNormalEvictFirstOp::register(ctx);
    CreatepolicyFractionalEvictUnchangedOp::register(ctx);
    CreatepolicyFractionalEvictUnchangedEvictFirstOp::register(ctx);
}
